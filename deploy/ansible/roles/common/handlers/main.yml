---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart netsentinel
  ansible.builtin.systemd:
    name: "{{ netsentinel_unit }}"
    state: restarted
    enabled: true
