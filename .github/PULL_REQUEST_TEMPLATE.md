## What & why

<!-- short description; link the issue -->

## Checklist

- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] `pytest` passes
- [ ] hub + sensor still import without root / nmap / scapy
- [ ] new config added to `Settings` **and** `.env.example`
- [ ] docs / CHANGELOG updated if behaviour changed
